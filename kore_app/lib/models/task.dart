class Task {
  int id;
  String title;
  bool isCompleted;
  String description;
  DateTime dueDate;
  DateTime dateCreated;
  int status;
  Task(this.id, this.title, this.isCompleted, this.description, this.dueDate);
}